# Git-Aware File Move

A skill that automatically moves files using `git mv` for git-tracked files to preserve history, or regular `mv` for untracked files.

Handles the decision logic automatically -- no need to check tracking status manually. Includes a Python script with dry-run, verbose, force-overwrite, and JSON output modes.
