# Changelog

## v1.0.0

### Features

- Add `--json` flag support to all actions (current, rename, list, show, delete)
- Initialize crush-session skill with session management script
- Support current session operations (get ID, rename)
- Support session listing and detailed view
- Support session deletion by ID (UUID, full hash, or prefix)
- Auto-detect Crush data directory from env var, project .crush folder, or default

### Stats

- 2 commits
- 1 Python script (178 lines)
- 0 external dependencies (stdlib only)
