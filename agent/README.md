# Agent Loader

A skill for loading custom agent definitions and activating them as system prompt overrides. Originally designed for Windsurf, it lets you define specialized agent personas in Markdown files and activate them as system prompt overrides for the current session. It can be used in Crush as well, though it was never adapted specifically for it.

## Features

- Load an agent by name without prompting
- Browse and select from available agents when no name is given
- Activate the chosen agent as a system prompt override
- Ship one example agent, `GROK-writing-assistant`, to demonstrate the format

## Prerequisites

- None beyond a standard agent runtime

## License

See the project root LICENSE.

This skill is kept here as an archived reference.